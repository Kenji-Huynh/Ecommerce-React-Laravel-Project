VITE_API_URL=http://127.0.0.1:8000
# Stripe test publishable key (public, safe to expose, but keep in env for flexibility)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Optional: display conversion rate if your DB is VND but UI shows USD
VITE_PRICE_CONVERT_VND_TO_USD_RATE=
